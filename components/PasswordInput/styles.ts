// Libs
import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  wrapper: {
    position: 'relative',
  },
  iconWrapper: {
    position: 'absolute',
    right: 2,
    top: 3,
    borderRadius: 100,
    paddingVertical: 7,
    paddingHorizontal: 4,
    textAlign: 'center',
    overflow: 'hidden',
  }
});
