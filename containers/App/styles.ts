// Libs
import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  loaderWrapper: {
    position: 'absolute',
    zIndex: 10,
    top: 0,
    right: 0,
    bottom: 0,
    left: 0,
    flex: 1,
    justifyContent: 'center',
  },
});
